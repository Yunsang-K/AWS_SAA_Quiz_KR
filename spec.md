# AWS SAA Quiz KR - 제품/기능 명세서 (spec)

## 1. 문서 목적
이 문서는 `AWS_SAA_Quiz_KR` 리포지토리의 현재 구현 상태를 분석하여,
앱의 목표, 사용자 흐름, 기능 요구사항, 데이터 모델, 실행/배포 방식, 제약사항을 정리한 **현행(as-is) 명세**다.

또한, 아래에 요청된 신규 요구사항(로그인, 모의고사, 오답 히스토리 정책, 학습 통계 시각화)을
**차기(to-be) 기능 명세**로 정식 반영한다.

---

## 2. 제품 개요
- 제품명: AWS Solution Architect Associate 퀴즈 웹앱
- 목적: AWS SAA-C03 준비를 위한 문제 풀이 및 오답 복습
- 형태: Vue 3 + Vite 기반 단일 페이지 애플리케이션(SPA)
- 주요 가치
  - 문제/선지/정답/해설 확인
  - 문제 탐색(이전/다음/번호 점프)
  - 오답 저장(localStorage) 및 오답만 보기

---

## 3. 대상 사용자
- AWS SAA 시험을 준비하는 한국어 사용자
- 브라우저 기반으로 빠르게 문제를 풀고 싶은 학습자

---

## 4. 기능 범위 (As-Is)

### 4.1 문제 풀이
- 문제 본문과 선지를 표시한다.
- 사용자는 선지 버튼을 클릭해 답안을 선택한다.
- 정답 개수(`answers.length`)만큼 선택되면 자동 채점한다.
- 채점 후:
  - 정답 선지는 `correct` 스타일
  - 사용자가 고른 오답 선지는 `incorrect` 스타일
  - 정답 텍스트(예: `A, C`)를 표시

### 4.2 해설 확인
- 채점 후 `해설 보기` 버튼을 통해 해설 목록을 노출한다.
- 해설은 복수 문장(배열) 렌더링을 지원한다.

### 4.3 문제 이동
- 이전 문제 / 다음 문제 버튼 제공
- 현재 문제 번호를 숫자로 입력해 특정 문제 인덱스로 이동 가능
- 유효하지 않은 번호 입력 시 경고창(alert) 표시

### 4.4 진행률 표시
- 상단 `progress` UI로 현재 진행 위치를 표시한다.
- `진행률: 현재번호 / 총문제수` 텍스트를 함께 표시한다.

### 4.5 오답 관리
- 오답 문제 ID를 `localStorage['wrongAnswers']`에 배열로 저장
- 오답 리스트를 화면에 표시
- `틀린 문제만 보기` 토글 기능 제공
  - ON: 오답 문제만 필터링하여 문제 집합 구성
  - OFF: 전체 문제 집합으로 복귀

---

## 5. 데이터 명세

### 5.1 문제 데이터 소스
- 파일: `src/questions.json`
- 총 문제 수: 724
- ID 범위: 1~725 (ID 662는 비어 있음)
- 중복 ID: 없음
- 단일정답: 638개
- 복수정답: 86개

### 5.2 문제 객체 스키마
```json
{
  "id": 1,
  "question": "문제 본문",
  "choices": {
    "A": "선지 A",
    "B": "선지 B",
    "C": "선지 C",
    "D": "선지 D"
  },
  "answers": ["A", "C"],
  "explanations": ["해설 1", "해설 2"]
}
```

### 5.3 오답 저장 스키마
- 키: `wrongAnswers`
- 타입: JSON 직렬화된 숫자 배열
- 예시: `[3, 17, 120]`

---

## 6. UI/상태 명세

### 6.1 핵심 상태
- `fullQuestions`: 원본 전체 문제 배열
- `questions`: 현재 표시 대상 문제 배열(전체 또는 오답 필터)
- `current`: 현재 문제 인덱스
- `selectedChoices`: 현재 문제에서 선택된 선지 배열
- `showAnswer`: 정답/채점 결과 표시 여부
- `showExplanation`: 해설 표시 여부
- `wrongAnswers`: 오답 문제 ID 목록
- `viewWrongOnly`: 오답만 보기 모드 여부
- `jumpNumber`: 번호 이동 입력값

### 6.2 상태 전이
- 문제 이동 시(`prev/next/goTo`) `resetState()`로 선택/정답/해설 상태 초기화
- `viewWrongOnly` 변경 시 watcher가 문제 집합 재구성 후 인덱스 0으로 리셋
- 채점 이후(`showAnswer=true`)에는 선지 선택이 비활성화됨

---

## 7. 기술 스택/구조
- 프레임워크: Vue 3 (Options API)
- 번들러/개발서버: Vite 5
- 엔트리: `src/main.js` → `App.vue` 마운트
- 스타일: `index.html` 전역 CSS + `App.vue` scoped CSS
- 저장소:
  - 기본: localStorage
  - 선택: Supabase(PostgreSQL) REST API 연동
- 라우터/상태관리 라이브러리: 사용하지 않음

---

## 8. 실행 및 배포 명세

### 8.1 로컬 실행
```bash
npm install
npm run dev
```
- 기본 접속: `http://localhost:5173`

### 8.2 프로덕션 빌드
```bash
npm run build
npm run preview
```

### 8.3 도커 실행
```bash
docker build -t aws-quiz-app .
docker run -p 5173:5173 aws-quiz-app
```

### 8.4 호스트 설정
- `vite.config.js`에서 `.up.railway.app` 허용 호스트를 dev/preview 양쪽에 설정

### 8.5 DB 연동 (선택)
- 환경변수 `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` 설정 시 DB 동기화 활성화
- 사용자 ID별 저장 데이터
  - 오답 목록
  - 누적 정답/풀이 수
  - 최근 정오답 이력
  - 오답 정책
- 미설정 시 localStorage-only 모드로 동작

---

## 9. 비기능 요구사항 (현행 기준)
- 브라우저 localStorage 사용 가능 환경 필요
- 네트워크 없이도 정적 파일 기준으로 문제 풀이 가능(문항이 번들 포함)
- 단일 클라이언트 앱으로 별도 백엔드/API 의존 없음

---

## 10. 확인된 제약/리스크
- 오답 기록은 **문제 최초 오답 여부만 저장**하며, 이후 정답으로 다시 풀어도 자동 제거되지 않음
- 정답 개수만큼 선택하면 즉시 채점되므로 “선택 후 제출” UX는 제공하지 않음
- 번호 이동은 현재 표시 집합(`questions`) 기준 인덱스로 동작하므로,
  오답 모드에서는 전체 문제의 ID와 입력 번호 체감이 다를 수 있음
- 문제 데이터가 대용량 단일 JSON 파일이라 향후 유지보수 시 분할 관리 필요 가능

---

## 11. 향후 개선 제안 (권장)
- 오답 히스토리(정답 전환 시 제거/유지 정책) 옵션화
- 문제 검색/태그/즐겨찾기/북마크 기능 추가
- 학습 통계(정답률, 최근 N문제 성과) 시각화
- 데이터 버전 관리(문항 출처/개정일) 메타데이터 도입
- 접근성 개선(키보드 포커스, ARIA, 색상 대비)

---

## 12. 차기 기능 요구사항 (To-Be, 정식 반영)

> 범위: Supabase Free 기반 DB 연동을 기본 옵션으로 제공하고, 환경변수 미설정 시 localStorage 단독 모드로 동작.

### 12.1 로그인 기능 추가 (ID 기반 + DB 연동)

#### 목적
- 사용자별 학습 상태(오답/통계/설정)를 분리해 저장하고 재사용한다.

#### 요구사항
- 로그인 방식은 ID 문자열 입력 기반으로 구현한다.
- 로그인 성공 시 `currentUser`를 localStorage에 저장하고, 동일 ID의 DB 레코드를 조회한다.
- 사용자 데이터는 로컬 캐시와 DB를 병행 저장한다.
  - 로컬: `awsQuiz:{username}:wrongAnswers`, `awsQuiz:{username}:stats`, `awsQuiz:{username}:settings`
  - DB: `quiz_user_state` 테이블(`user_id` PK)
- 로그아웃 시 화면 상태는 초기화하고, 로컬/DB 저장 데이터는 유지한다.

#### 제약 및 보안 고지
- 현재 구현은 ID 문자열 중심의 간이 로그인이다.
- 운영 환경에서는 Supabase Auth + RLS 고도화가 필요하다.

### 12.2 모의고사 기능 추가 (랜덤 N문항)

#### 목적
- 실전 대비를 위해 사용자가 지정한 문항 수(N)만큼 랜덤 출제 세션을 제공한다.

#### 요구사항
- 사용자는 모의고사 시작 전 출제 문항 수 `N`을 입력한다.
- 시스템은 전체 문항 풀에서 **중복 없이 랜덤으로 N문항**을 추출한다.
- 모의고사 모드에서는 아래를 표시한다.
  - 현재 문항 번호 / N
  - 남은 문항 수
  - 제출 완료 여부
- 모의고사 종료 시 결과 요약을 제공한다.
  - 점수(정답 수 / N)
  - 정답률(%)
  - 오답 문항 ID 목록
- 모의고사 결과는 사용자별 통계 데이터에 누적 저장한다.

#### 예외 처리
- N이 1 미만이거나 전체 문항 수보다 큰 경우 시작 불가 및 입력 안내 메시지 표시.

### 12.3 정답률 기록/학습 통계 시각화

#### 저장 지표
- 사용자별 누적 지표를 기록한다.
  - 총 풀이 문항 수
  - 총 정답 수
  - 누적 정답률
  - 최근 N문항(기본 20) 정답률
  - 모의고사 이력(응시일, 문항 수, 점수, 정답률)

#### 시각화 요구사항
- 대시보드 영역에 최소 아래 항목을 시각화한다.
  - 누적 정답률(숫자 + 프로그레스바)
  - 최근 N문항 정답률(숫자 + 간단 막대/라인)
  - 최근 모의고사 결과 목록(최신순)

### 12.4 오답 히스토리 정책 옵션화

#### 목적
- 학습 스타일에 맞춰 오답 관리 정책을 선택 가능하게 한다.

#### 정책 옵션
- `keep_history` (기본값): 한 번 틀린 문제는 이후 맞춰도 오답 목록에 유지
- `remove_on_correct`: 이후 정답 처리 시 오답 목록에서 자동 제거

#### 저장
- 정책 설정은 사용자별 `settings`에 저장하고 재접속 시 복원한다.

### 12.5 권장 구현 순서
1. 간이 로그인/프로필 분리 저장소 도입
2. 통계 저장 모델 도입(기존 채점 흐름과 연결)
3. 모의고사 랜덤 출제 모드 추가
4. 통계 시각화 UI 추가
5. 오답 히스토리 정책 토글 및 기존 오답 처리 로직 분기
